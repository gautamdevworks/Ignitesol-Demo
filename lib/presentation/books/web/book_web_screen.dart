import 'package:flutter/material.dart';

class BookWebScreen extends StatelessWidget {
      const BookWebScreen({super.key});
    
      @override
      Widget build(BuildContext context) {
        return Container();
      }
    }