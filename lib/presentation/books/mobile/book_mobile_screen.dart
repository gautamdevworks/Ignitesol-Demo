import 'package:flutter/material.dart';

class BookMobileScreen extends StatelessWidget {
  const BookMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}